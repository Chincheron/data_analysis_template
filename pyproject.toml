[project]
name = "data-analysis-template"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyprojroot",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["config*", "src*"]